# Copy to .env.local and fill in from your Supabase project
# (Project Settings -> API). Publishable/anon key ONLY — never the
# service-role key. Leave both empty to run in JSON-only mode
# (no database or sign-in features).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
